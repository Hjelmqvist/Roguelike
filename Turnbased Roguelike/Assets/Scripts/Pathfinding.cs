using System.Collections.Generic;
using UnityEngine;

public static class Pathfinding
{
    public static bool TryGetPath(Vector2Int[,] grid, Vector2Int start, Vector2Int end, out List<Vector2Int> path)
    {
        path = new List<Vector2Int>();

        // Implement A* pathfinding here :)
        // Only need vertical and horizontal checks

        return true;
    }
}